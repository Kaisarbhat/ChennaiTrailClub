export const metadata = {
  title: `Upcoming Events - Chennai Trail Club`,
  description: "Chennai Trail Club Upcoming Events Page",
};

export default function UpcomingEventsLayout({ children }) {
  return <>{children}</>;
}
