export const metadata = {
  title: `Events - Chennai Trail Club`,
  description: "Chennai Trail Club Events Page",
};

export default function EventsLayout({ children }) {
  return <>{children}</>;
}
